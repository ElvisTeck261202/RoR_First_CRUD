<div class="container-fluid bg-primary text-white" style="padding:1.25em"><h1 class="display-6 text-center ">Article</h1></div>

<div class="container-fluid row justify-content-center mt-2">
<h1 class="text-center"><%= @article.title %></h1>

<p class="text-center"><%= @article.body %></p>

  <div class="btn btn-outline-warning m-2 w-25"><%= link_to "Edit", edit_article_path(@article) %></div><br>
  <div class="btn btn-outline-danger m-2 w-25"><%= link_to "Destroy", article_path(@article), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></div>

  <a class="btn btn-outline-warning row justify-content-center w-50 rounded" href="/articles">Back</a>

</div>